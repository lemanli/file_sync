# File Sync 0.1.4

支持任务暂停/恢复、暂停后参数调整及追加目录组。已有目录地址及包含源目录名选项锁定；限速、重试和遇错继续对后续文件生效，并发及批量参数从下一目录组生效。

暂停等待正在处理的文件完成，保留本进程扫描位置。退出或升级后不能恢复内存游标，任务记为中断，重新运行按配置比较已有文件。

`scripts/migrate_data.py` 迁移旧单机数据库，包含任务、历史、日志和 WAL 中已提交数据，拒绝覆盖已有目标库。使用说明见 docs/INSTALL.md。

本版未实现内存预算和百万文件扫描加速；75% 的内存口径尚待确认。
