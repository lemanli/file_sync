# 暂停、参数调整与迁移

- 文件边界暂停，扫描循环设检查点；等待当前文件完成，恢复保留当前扫描及线程池。
- 暂停时仅允许限速、重试、遇错继续、名称、下一目录组并发/批量参数及追加目录；既有目录及同步语义锁定。
- 每次暂停、恢复、修改写入执行日志；退出时唤醒并中断暂停任务，重启后保留历史但不声称恢复内存游标。
- SQLite backup 迁移任务、执行历史和日志，拒绝覆盖既有目标数据库，禁止源存在活动任务。
- 验证暂停期间没有新复制、恢复参数及追加组生效、旧目录修改被拒绝、迁移完整性，最后重新打包。
