# 目录扫描性能改造计划

目标和决策见 CURRENT_SCAN_ANALYSIS.md。按以下顺序实施并记录结果：
1. 四种策略的失败契约测试；统一最小字段扫描器，原生布局与可移植等价测试。
2. 有界目录快照和目录工作队列；引擎复用现有复制、安全、暂停和删除逻辑。
3. API/表单配置和实时指标；旧任务 AUTO 兼容。
4. 本地扫描/端到端基准，平台与网络未测矩阵；全套回归、打包冒烟。
5. 更新结果文档、版本/变更日志；提交并推送 GitHub，检查 CI。
