# File Sync 0.1.7

单向目录扫描与比较架构升级。升级后旧任务继续 AUTO，不改动比较语义；编辑任务选择“按目录比较大小和时间”启用优化。全部复制不扫描目标；跳过已有名称不取大小时间。

SQLite 数据目录与 0.1.6 兼容，无需手动迁移表；继续使用 application.yml 的固定本地数据目录，先退出旧进程再启动。支持原有 Windows CMD、自定义 Python/venv 和镜像源。

性能、平台验证范围及基准命令见 [扫描优化报告](../SCAN_OPTIMIZATION_RESULT.md)。不是实时同步，不建立持久文件索引。
