# 更新日志

## 0.1.7 — 2026-09-23

- 跳过已有策略使用原子不覆盖发布，防止覆盖扫描后出现的目标；源名称等价冲突与目录名等价检查在操作前保护。

- 对 SQLite 3.51.0/3.51.1 增加短事务串行兼容，规避回归捕获的 Unix 并发打开/关闭死锁。

- 新增 COPY_ALL / SKIP_EXISTING / COMPARE_METADATA / MIRROR，扫描字段随策略变化；原有任务 AUTO 保持语义。
- 按目录快照比较、独立扫描并发、有界复制批次；大目录、目录队列与镜像删除计划使用一次性临时缓冲，不建立持久索引。
- macOS 批量属性枚举；Windows 两个可选原生枚举后端；Linux/通用 scandir。能力不支持时整目录安全回退。
- COPY_ALL 不枚举目标（包括权限预检）；COMPARE_METADATA 不逐文件查询目标进行比较。保留写入、链接、源变化和删除安全复核。
- 增加两侧扫描速率、属性/枚举 API 计数与队列指标、性能分析报告和可重复基准。
- 目录流水线复用暂停、失败重试、网络自动恢复、执行报告和日志。

- Windows 双击入口直接使用 CMD 调用 Python，无需 PowerShell；支持配置解释器和透传安装参数。
## 0.1.6 — 2026-09-23

- 单向预检使用 scandir 的条目类型，减少逐文件 lstat；空忽略规则直接返回，单向执行不再保存全部已见文件集合。
- 预检进度批量更新，显示实际扫描条目/秒、源或目标端及当前目录。
- 开启遇错继续时，网络断开、超时和存储 I/O 故障停止当前组后续操作，按分钟等待重试；默认间隔 3 分钟、3 次自动重试。
- 网络重试用尽后保持暂停，管理员恢复后开启新一轮；可手动暂停等待或立即重试，退出会中断等待。
- 重试重新比较当前组，已完成文件按配置跳过，执行统计累计保留先前成功的复制，日志记录重试原因和次数。
- 补充可重复扫描基准、连接故障恢复和安全边界回归验证。

## 0.1.5 — 2026-09-23

- 修复直接关闭终端后，历史 running/paused 状态导致迁移被拒绝的问题：副本标记为中断，逐执行记录原因，原库不改写。
- application.yml 支持固定数据目录和首次启动自动迁移，目标已有数据库时直接复用，不重复覆盖。
- 新增 migrate-data.cmd，双击读取配置即可准备数据，无需修改脚本或输入路径参数。
- 新版启动和迁移使用操作系统独占锁，进程退出自动释放，防止多个新版程序同时使用同一数据目录。
- 本机多版本可轮流共用数据目录；旧版没有进程锁，首次切换须先退出旧程序。

## 0.1.4 — 2026-09-22

- 新增文件边界暂停和恢复，等待当前文件完成，保留本次进程内扫描位置。
- 暂停时允许修改限速、重试、遇错继续及下一组并发参数，并允许追加目录组；锁定既有目录和同步语义。
- 暂停、恢复、参数变更写入日志；退出唤醒暂停任务并记录中断。
- 新增 SQLite 安全迁移脚本，保留任务、历史和日志，包含 WAL 数据且拒绝覆盖目标库。
- 更新前后端能力校验，避免旧后台忽略暂停与新配置。

## 0.1.3 — 2026-09-22

- 单向且关闭删除时允许多组共用或嵌套目标，按目录组顺序执行。
- 每组可选择包含源目录名，界面即时预览实际目标；日志和权限预检使用实际目标。
- 删除、双向及目标影响其他源的重叠配置继续拦截。

## 0.1.2 — 2026-09-22

- 修复目录安装、隔离模式 Python 启动时找不到 standalone 模块的问题。
- 启动冒烟测试使用 Python 隔离模式，并验证解压安装包。
- 保留 CMD 直接启动、自定义 Python 和网络共享文件标志兼容修复。

## 0.1.1 — 2026-09-22

- Windows PowerShell 脚本使用 UTF-8 BOM 和 CRLF，增加 Windows PowerShell 5.1 启动检查。
- 统一源码打包脚本，从当前工程生成 ZIP、tar.gz 和 SHA256SUMS，包含未提交修复。

- 修复 macOS 文件同步到网络共享时，复制 BSD 文件标志导致 Errno 22；仍保留权限和修改时间校验。

- 安装支持 direct/venv 两种模式，允许使用未注册 PATH 的独立 Python 目录。
- Windows 启动器统一读取解释器配置，支持绝对路径、工程相对路径和带空格路径。
- 使用所选解释器的 pip，已有 pip 时不再要求 ensurepip；直接模式不要求 venv。
- 补充安装配置示例及直接安装回归验证。

## 0.1.0 — 2026-09-22

- 提取为独立单机工程，保留原有MIT许可证。
- Windows双击启动，自动环境检查、安装修复及镜像选择。
- 多目录单向/双向同步、忽略规则、重试、演练及权限预检。
- 大小/时间/SHA-256比较、修改时间保留、临时文件发布。
- 实时阶段、活动动画、报告统计、服务端日志筛选与跳页。
- 隔离临时目录测试、跨平台启动检查和源码发布工作流。
